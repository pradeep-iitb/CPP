# Data Type Modifiers

* long 
* short
* long long
* signed can store both positive and negative no
* unsigned stores onlypositive no . when stored negative no interpreter treats the 2's complement of negative number and converts it into decmal directly

# Functions 
Any function is executed till only the first return .

